# Деплой на TimeWeb.cloud через GitHub Actions

Это руководство поможет вам настроить автоматический деплой вашего Next.js приложения на TimeWeb.cloud при каждом push в ветку `main`.

## 📋 Предварительные требования

1. **Аккаунт на TimeWeb.cloud** с доступом к VPS или облачному серверу
2. **SSH доступ** к серверу TimeWeb.cloud
3. **Node.js** установлен на сервере (версия 20 или выше)
4. **PM2** (опционально, но рекомендуется) для управления процессом Node.js
5. **GitHub репозиторий** с вашим проектом

## 🔧 Настройка сервера TimeWeb.cloud

### 1. Подготовка сервера

Подключитесь к серверу через SSH и выполните:

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Node.js 20
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# Установка PM2 (рекомендуется)
sudo npm install -g pm2

# Создание директории для деплоя
mkdir -p /var/www/deztechug
cd /var/www/deztechug

# Установка прав доступа
sudo chown -R $USER:$USER /var/www/deztechug
```

### 2. Настройка PM2 (опционально)

Проект использует **standalone режим** Next.js (`output: 'standalone'`), что означает, что после сборки создается минимальный сервер с файлом `server.js`.

Создайте файл `ecosystem.config.js` на сервере:

```bash
cd /var/www/deztechug
nano ecosystem.config.js
```

**Для standalone режима** (рекомендуется):

```javascript
module.exports = {
  apps: [{
    name: 'deztechug',
    script: 'server.js',
    cwd: '/var/www/deztechug',
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '1G',
    env: {
      NODE_ENV: 'production',
      PORT: 3000,
      HOSTNAME: '0.0.0.0'
    }
  }]
};
```

**Альтернативный вариант** (если standalone не используется):

```javascript
module.exports = {
  apps: [{
    name: 'deztechug',
    script: 'npm',
    args: 'start',
    cwd: '/var/www/deztechug',
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '1G',
    env: {
      NODE_ENV: 'production',
      PORT: 3000
    }
  }]
};
```

Запустите приложение:

```bash
pm2 start ecosystem.config.js
pm2 save
pm2 startup  # Следуйте инструкциям для автозапуска при перезагрузке
```

### 3. Настройка Nginx (рекомендуется)

Если вы хотите использовать Nginx как reverse proxy:

```bash
sudo apt install nginx -y
```

Создайте конфигурацию:

```bash
sudo nano /etc/nginx/sites-available/deztechug
```

Содержимое:

```nginx
server {
    listen 80;
    server_name ваш-домен.ru www.ваш-домен.ru;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

Активируйте конфигурацию:

```bash
sudo ln -s /etc/nginx/sites-available/deztechug /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

## 🔐 Настройка GitHub Secrets

Для работы автоматического деплоя нужно добавить следующие секреты в ваш GitHub репозиторий:

1. Перейдите в ваш репозиторий на GitHub
2. Откройте **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret** и добавьте следующие секреты:

### Обязательные секреты:

- **`TIMEWEB_HOST`** - IP адрес или доменное имя вашего сервера TimeWeb.cloud
  - Пример: `123.45.67.89` или `server.timeweb.cloud`

- **`TIMEWEB_USERNAME`** - Имя пользователя для SSH подключения
  - Пример: `root` или `deploy`

- **`TIMEWEB_SSH_KEY`** - Приватный SSH ключ для подключения к серверу
  - Как получить:
    ```bash
    # На вашем локальном компьютере
    ssh-keygen -t ed25519 -C "github-actions"
    # Скопируйте содержимое приватного ключа (~/.ssh/id_ed25519)
    # И публичный ключ добавьте на сервер:
    cat ~/.ssh/id_ed25519.pub | ssh username@your-server "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
    ```

- **`TIMEWEB_DEPLOY_PATH`** - Путь на сервере, куда будет деплоиться приложение
  - Пример: `/var/www/deztechug`

### Опциональные секреты:

- **`TIMEWEB_SSH_PORT`** - Порт SSH (по умолчанию 22)
  - Пример: `2222`

## 🚀 Использование

### Автоматический деплой

После настройки всех секретов, каждый push в ветку `main` автоматически запустит деплой:

```bash
git add .
git commit -m "Обновление сайта"
git push origin main
```

### Ручной запуск деплоя

Вы также можете запустить деплой вручную:

1. Перейдите в **Actions** в вашем GitHub репозитории
2. Выберите workflow **Deploy to TimeWeb.cloud**
3. Нажмите **Run workflow**
4. Выберите ветку и нажмите **Run workflow**

## 📝 Структура деплоя

Workflow выполняет следующие шаги:

1. ✅ Проверка кода из репозитория
2. ✅ Установка Node.js и зависимостей
3. ✅ Сборка Next.js приложения (`npm run build`)
4. ✅ Создание пакета для деплоя
5. ✅ Загрузка файлов на сервер через SCP
6. ✅ Установка зависимостей на сервере (если нужно)
7. ✅ Перезапуск приложения через PM2

## 🔍 Проверка деплоя

После успешного деплоя:

1. Проверьте логи в GitHub Actions
2. Проверьте статус приложения на сервере:
   ```bash
   ssh username@your-server
   pm2 status
   pm2 logs deztechug
   ```
3. Откройте ваш сайт в браузере

## 🐛 Решение проблем

### Ошибка подключения по SSH

- Проверьте правильность `TIMEWEB_HOST` и `TIMEWEB_USERNAME`
- Убедитесь, что SSH ключ добавлен правильно
- Проверьте, что порт SSH указан верно

### Ошибка при сборке

- Проверьте, что все зависимости указаны в `package.json`
- Убедитесь, что версия Node.js совместима (20+)

### Приложение не запускается

- Проверьте логи PM2: `pm2 logs deztechug`
- Убедитесь, что порт 3000 не занят другим процессом
- Проверьте переменные окружения

### Проблемы с правами доступа

```bash
# На сервере
sudo chown -R $USER:$USER /var/www/deztechug
chmod -R 755 /var/www/deztechug
```

## 📚 Дополнительные ресурсы

- [Документация TimeWeb.cloud](https://timeweb.cloud/docs/)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [PM2 Documentation](https://pm2.keymetrics.io/docs/)
- [Next.js Deployment](https://nextjs.org/docs/deployment)

## 🔒 Безопасность

- ⚠️ **Никогда не коммитьте секреты в репозиторий**
- ✅ Используйте GitHub Secrets для всех чувствительных данных
- ✅ Регулярно обновляйте зависимости: `npm audit fix`
- ✅ Используйте SSH ключи вместо паролей
- ✅ Настройте firewall на сервере

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи GitHub Actions
2. Проверьте логи на сервере (PM2, Nginx)
3. Убедитесь, что все секреты настроены правильно
4. Проверьте документацию TimeWeb.cloud

---

**Успешного деплоя! 🚀**
