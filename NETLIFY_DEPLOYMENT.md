# Руководство по деплою DezTehUg на Netlify

## Обзор проекта

**DezTehUg** - это современный сайт санитарных услуг с cyberpunk дизайном, построенный на Next.js 14.0.4 с TypeScript и App Router. Проект полностью подготовлен для статического деплоя на Netlify.

## Предварительные требования

### ✅ Техническая подготовка завершена
- ✅ Next.js конфигурация оптимизирована для Netlify
- ✅ Статический экспорт настроен (`output: 'export'`)
- ✅ Файл `netlify.toml` создан с полной конфигурацией
- ✅ Метаданные и SEO оптимизированы
- ✅ Build процесс протестирован и работает корректно
- ✅ Все 11 страниц генерируются как статический HTML

### 📋 Что вам понадобится
- Аккаунт на [Netlify](https://netlify.com)
- Репозиторий проекта на GitHub (уже настроен)
- Доступ к настройкам домена (опционально)

## Пошаговое руководство по деплою

### Шаг 1: Подготовка репозитория

1. **Убедитесь, что все изменения зафиксированы в Git:**
   ```bash
   git status
   git add .
   git commit -m "feat: Подготовка к деплою на Netlify"
   git push origin main
   ```

2. **Проверьте, что build работает локально:**
   ```bash
   npm run build
   ```
   Должно создаться 11 статических страниц в папке `out/`

### Шаг 2: Создание сайта на Netlify

1. **Войдите в Netlify:**
   - Перейдите на [netlify.com](https://netlify.com)
   - Войдите через GitHub аккаунт

2. **Создайте новый сайт:**
   - Нажмите "Add new site" → "Import an existing project"
   - Выберите "Deploy with GitHub"
   - Найдите и выберите репозиторий `DezTehUg.new-main`

3. **Настройте параметры деплоя:**
   ```
   Branch to deploy: main
   Build command: npm run netlify
   Publish directory: out
   ```

4. **Нажмите "Deploy site"**

### Шаг 3: Настройка домена (опционально)

1. **Настройка поддомена Netlify:**
   - В настройках сайта перейдите в "Site settings" → "Domain management"
   - Нажмите "Options" → "Edit site name"
   - Введите желаемое имя: `deztehug` (получится `deztehug.netlify.app`)

2. **Подключение собственного домена:**
   - В "Domain management" нажмите "Add custom domain"
   - Введите ваш домен (например, `deztehug.ru`)
   - Следуйте инструкциям по настройке DNS записей

### Шаг 4: Настройка переменных окружения

В настройках сайта перейдите в "Environment variables" и добавьте:

```
NODE_VERSION=18
NPM_VERSION=9
NEXT_TELEMETRY_DISABLED=1
```

### Шаг 5: Проверка деплоя

1. **Дождитесь завершения деплоя** (обычно 2-5 минут)
2. **Проверьте основные страницы:**
   - Главная: `https://ваш-сайт.netlify.app/`
   - О компании: `https://ваш-сайт.netlify.app/about`
   - Калькулятор: `https://ваш-сайт.netlify.app/calculator`
   - Услуги: `https://ваш-сайт.netlify.app/services`

3. **Проверьте страницы услуг:**
   - Дезинфекция: `https://ваш-сайт.netlify.app/services/disinfection`
   - Дезинсекция: `https://ваш-сайт.netlify.app/services/pest-control`
   - Дератизация: `https://ваш-сайт.netlify.app/services/deratization`
   - Анализ воды: `https://ваш-сайт.netlify.app/services/water-analysis`

## Конфигурационные файлы

### next.config.js
```javascript
const nextConfig = {
  output: 'export',
  eslint: { ignoreDuringBuilds: true },
  images: { unoptimized: true },
  trailingSlash: false,
  // Security headers configured in netlify.toml for static export
};

module.exports = nextConfig;
```

### netlify.toml
```toml
[build]
  command = "npm run netlify"
  publish = "out"

[build.environment]
  NODE_VERSION = "18"
  NPM_VERSION = "9"

# URL redirects for service pages
[[redirects]]
  from = "/services/disinfection"
  to = "/services/disinfection.html"
  status = 200

[[redirects]]
  from = "/services/pest-control"
  to = "/services/pest-control.html"
  status = 200

[[redirects]]
  from = "/services/deratization"
  to = "/services/deratization.html"
  status = 200

[[redirects]]
  from = "/services/water-analysis"
  to = "/services/water-analysis.html"
  status = 200

# Main page redirects
[[redirects]]
  from = "/about"
  to = "/about.html"
  status = 200

[[redirects]]
  from = "/calculator"
  to = "/calculator.html"
  status = 200

[[redirects]]
  from = "/services"
  to = "/services.html"
  status = 200

# 404 handling
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline' fonts.googleapis.com; font-src 'self' fonts.gstatic.com; img-src 'self' data: https:; connect-src 'self';"

# Caching for static assets
[[headers]]
  for = "/_next/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/icons/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000"

# Font optimization
[[headers]]
  for = "*.woff2"
  [headers.values]
    Cache-Control = "public, max-age=31536000"
    Access-Control-Allow-Origin = "*"
```

### package.json (scripts)
```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "netlify": "next build",
    "start": "next start",
    "lint": "next lint"
  }
}
```

## Автоматические деплои

После первоначальной настройки Netlify будет автоматически деплоить сайт при каждом push в ветку `main`:

1. **Внесите изменения в код**
2. **Зафиксируйте изменения:**
   ```bash
   git add .
   git commit -m "feat: Описание изменений"
   git push origin main
   ```
3. **Netlify автоматически запустит новый деплой**

## Мониторинг и логи

### Просмотр логов деплоя
1. В панели Netlify перейдите в "Deploys"
2. Кликните на нужный деплой
3. Просмотрите логи в разделе "Deploy log"

### Мониторинг производительности
- Netlify автоматически предоставляет метрики производительности
- Проверьте "Site analytics" для статистики посещений
- Используйте "Functions" для мониторинга серверных функций (если добавите)

## Устранение неполадок

### Проблема: Build fails with "Cannot find module"
**Решение:**
```bash
# Очистите кэш и переустановите зависимости
rm -rf node_modules package-lock.json
npm install
npm run build
```

### Проблема: 404 ошибки на страницах услуг
**Решение:** Проверьте, что в `netlify.toml` настроены правильные redirects для всех страниц услуг.

### Проблема: Медленная загрузка изображений
**Решение:** В `next.config.js` уже настроено `images: { unoptimized: true }` для статического экспорта.

### Проблема: CSP ошибки в консоли
**Решение:** Настройте Content-Security-Policy в `netlify.toml` под ваши нужды.

### Проблема: Шрифты не загружаются
**Решение:** Убедитесь, что в CSP разрешены `fonts.googleapis.com` и `fonts.gstatic.com`.

## Оптимизация производительности

### Уже реализовано:
- ✅ Статическая генерация всех страниц
- ✅ Оптимизация изображений для статического экспорта
- ✅ Кэширование статических ресурсов
- ✅ Минификация CSS и JavaScript
- ✅ Сжатие шрифтов и ресурсов

### Дополнительные оптимизации:
1. **Включите Netlify Analytics** для детального мониторинга
2. **Настройте Netlify Forms** для форм обратной связи
3. **Добавьте Netlify Functions** для серверной логики (если нужно)

## Безопасность

### Уже настроено:
- ✅ Security headers (CSP, X-Frame-Options, etc.)
- ✅ HTTPS по умолчанию
- ✅ Защита от XSS атак
- ✅ Referrer Policy

### Дополнительные меры:
1. **Настройте Branch Protection** в GitHub
2. **Включите Deploy Previews** для тестирования изменений
3. **Настройте Webhook notifications** для мониторинга деплоев

## Поддержка и обновления

### Регулярное обслуживание:
1. **Обновляйте зависимости** ежемесячно:
   ```bash
   npm audit
   npm update
   ```

2. **Мониторьте производительность** через Netlify Analytics

3. **Проверяйте логи деплоев** на наличие предупреждений

### Контакты для поддержки:
- **Netlify Support**: [support.netlify.com](https://support.netlify.com)
- **Next.js Documentation**: [nextjs.org/docs](https://nextjs.org/docs)
- **Проектная документация**: `.kilocode/rules/memory-bank/`

---

## Временное отключение деплоя

В некоторых ситуациях может потребоваться временно отключить деплой сайта на Netlify. Это может быть необходимо для проведения технического обслуживания, критических обновлений или в случае обнаружения проблем в продакшене.

### Метод 1: Полное отключение сайта

**Когда использовать:** Для полного технического обслуживания или критических исправлений.

#### Отключение:
1. **Войдите в панель Netlify**
2. **Перейдите в настройки сайта:**
   - Site settings → General → Site details
3. **Остановите сайт:**
   - Нажмите "Stop auto publishing"
   - Подтвердите действие
4. **Опционально - добавьте страницу обслуживания:**
   - Site settings → Build & deploy → Post processing
   - Включите "Asset optimization"
   - Добавьте кастомную 503 страницу

#### Повторное включение:
1. **Вернитесь в Site settings → General**
2. **Нажмите "Start auto publishing"**
3. **Запустите новый деплой:**
   - Deploys → Trigger deploy → Deploy site

### Метод 2: Приостановка автоматических деплоев

**Когда использовать:** Когда нужно остановить автоматические деплои, но оставить сайт доступным.

#### Приостановка:
1. **Перейдите в Build settings:**
   - Site settings → Build & deploy → Continuous deployment
2. **Остановите автоматические деплои:**
   - Нажмите "Stop builds"
   - Выберите "Stop auto publishing"
3. **Сайт останется доступным, но новые коммиты не будут деплоиться**

#### Возобновление:
1. **Вернитесь в Build settings**
2. **Нажмите "Start auto publishing"**
3. **Новые коммиты снова будут автоматически деплоиться**

### Метод 3: Использование переменной окружения

**Когда использовать:** Для программного контроля доступности сайта без остановки деплоев.

#### Настройка переменной:
1. **Добавьте переменную окружения:**
   - Site settings → Environment variables
   - Добавьте: `SITE_MAINTENANCE=true`

2. **Обновите код приложения** (добавьте в `app/layout.tsx`):
```typescript
// Проверка режима обслуживания
if (process.env.SITE_MAINTENANCE === 'true') {
  return (
    <html lang="ru">
      <body>
        <div style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          minHeight: '100vh',
          backgroundColor: '#0a0a0a',
          color: '#39FF14',
          fontFamily: 'Orbitron, monospace',
          textAlign: 'center',
          padding: '20px'
        }}>
          <h1 style={{ fontSize: '2rem', marginBottom: '1rem' }}>
            🔧 Техническое обслуживание
          </h1>
          <p style={{ fontSize: '1.2rem', marginBottom: '2rem' }}>
            Сайт временно недоступен для проведения технических работ
          </p>
          <p style={{ color: '#FF6B35' }}>
            Ожидаемое время восстановления: в течение часа
          </p>
        </div>
      </body>
    </html>
  );
}
```

3. **Зафиксируйте изменения и задеплойте**

#### Отключение режима обслуживания:
1. **Удалите или измените переменную:**
   - Установите `SITE_MAINTENANCE=false` или удалите переменную
2. **Новый деплой автоматически восстановит нормальную работу**

### Альтернативные решения

#### Использование ветки для обслуживания:
1. **Создайте ветку maintenance:**
   ```bash
   git checkout -b maintenance
   git push origin maintenance
   ```

2. **Измените ветку для деплоя:**
   - Site settings → Build & deploy → Continuous deployment
   - Измените "Branch to deploy" с `main` на `maintenance`

3. **Для восстановления:**
   - Верните ветку деплоя обратно на `main`

#### Использование Netlify redirects:
Добавьте в `netlify.toml`:
```toml
# Временное перенаправление на страницу обслуживания
[[redirects]]
  from = "/*"
  to = "/maintenance.html"
  status = 200
  force = true
```

### Важные предупреждения

⚠️ **SEO воздействие:**
- Длительное отключение может негативно повлиять на SEO рейтинг
- Используйте HTTP статус 503 для временного обслуживания
- Добавьте мета-тег `Retry-After` для поисковых систем

⚠️ **Уведомление пользователей:**
- Предупредите пользователей заранее о планируемом обслуживании
- Укажите примерное время восстановления
- Предоставьте альтернативные способы связи

⚠️ **Мониторинг:**
- Следите за логами деплоев во время восстановления
- Проверьте все критические функции после включения
- Убедитесь, что все переменные окружения корректны

### Рекомендации

1. **Планируйте обслуживание** в часы минимальной активности пользователей
2. **Тестируйте процедуры** отключения/включения на тестовом сайте
3. **Документируйте причины** отключения для команды
4. **Подготовьте план восстановления** заранее
5. **Уведомляйте заинтересованные стороны** о статусе работ

---

## Заключение

Проект DezTehUg полностью подготовлен для деплоя на Netlify. Все конфигурационные файлы настроены, build процесс оптимизирован, и статические файлы генерируются корректно.

После деплоя у вас будет:
- ⚡ Быстрый статический сайт с cyberpunk дизайном
- 🔒 Безопасная конфигурация с security headers
- 📱 Полностью адаптивный дизайн для всех устройств
- 🎨 Сохраненная cyberpunk эстетика с оптимизированной читаемостью
- 📊 SEO оптимизация для всех страниц услуг

**Время деплоя:** 5-10 минут  
**Статус готовности:** ✅ Готов к деплою  
**Последнее тестирование:** Успешно (11 статических страниц сгенерированы)

---

*Создано: 15 июля 2025, 23:09 (UTC+3)*
*Обновлено: 15 июля 2025, 23:56 (UTC+3)*
*Версия руководства: 1.1.0*
*Статус проекта: Готов к продакшену*