# 📋 Список всех инструкций проекта DezTehUg

Этот документ содержит полный список всех инструкций, найденных в проекте.

---

## 1. 🚀 Запуск локального сервера разработки

### Инструкция:
- **Команда**: `npm run dev`
- **Описание**: Запускает Next.js dev сервер на localhost:3000
- **Файл**: `package.json` (script: `dev`)

### Предварительные требования:
1. Установить зависимости: `npm install`
2. Убедиться, что порт 3000 свободен

---

## 2. 🔧 Активация GitHub Copilot

### Файл: `COPILOT-ACTIVATION.md`

#### Шаг 1: Проверка статуса
- Открыть Command Palette (`Ctrl+Shift+P`)
- Ввести: `GitHub Copilot: Check Status`
- Проверить статус подписки и авторизации

#### Шаг 2: Авторизация в GitHub
- Command Palette → `GitHub Copilot: Sign In`
- Следовать инструкциям для входа в GitHub
- Подтвердить авторизацию в браузере

#### Шаг 3: Включение Copilot
- Command Palette → `GitHub Copilot: Enable`
- Проверить статус-бар внизу VS Code (должна быть иконка Copilot)

#### Шаг 4: Настройка автодополнения
```json
{
  "github.copilot.enable": {
    "*": true
  },
  "editor.inlineSuggest.enabled": true
}
```

#### Шаг 5: Проверка работы
- Открыть файл `copilot-test.tsx`
- Начать печатать после TODO комментариев
- Copilot должен предложить автодополнение (серый текст)
- Нажать `Tab` для принятия предложения

### Горячие клавиши:
- `Tab` - принять предложение
- `Esc` - отклонить предложение
- `Alt+]` - следующее предложение
- `Alt+[` - предыдущее предложение
- `Ctrl+Shift+I` - открыть Copilot Chat

---

## 3. 🔐 Вход в GitHub для активации Copilot

### Файл: `github-signin-guide.md`

#### Шаг 1: Открыть Command Palette
1. Нажать `Ctrl+Shift+P` (или `Cmd+Shift+P` на Mac)
2. В поисковой строке ввести: `GitHub Copilot: Sign In`
3. Нажать Enter

#### Шаг 2: Авторизация через браузер
1. VS Code откроет браузер с страницей авторизации GitHub
2. Если не вошли в GitHub - ввести логин и пароль
3. Подтвердить авторизацию для VS Code
4. Разрешить доступ к GitHub Copilot

#### Шаг 3: Подтверждение в VS Code
1. Вернуться в VS Code
2. Должно появиться уведомление об успешной авторизации
3. В статус-баре (внизу) должна появиться иконка Copilot

#### Альтернативный способ:
1. Открыть Command Palette (`Ctrl+Shift+P`)
2. Ввести: `GitHub: Sign In`
3. Выбрать "Sign in with GitHub"
4. Следовать инструкциям в браузере

---

## 4. 🌐 Деплой на Netlify

### Файл: `NETLIFY_DEPLOYMENT.md`

#### Шаг 1: Подготовка репозитория
```bash
git status
git add .
git commit -m "feat: Подготовка к деплою на Netlify"
git push origin main
```

#### Шаг 2: Проверка локального build
```bash
npm run build
```
Должно создаться 11 статических страниц в папке `out/`

#### Шаг 3: Создание сайта на Netlify
1. Войти в Netlify: [netlify.com](https://netlify.com)
2. Войти через GitHub аккаунт
3. Нажать "Add new site" → "Import an existing project"
4. Выбрать "Deploy with GitHub"
5. Найти и выбрать репозиторий `DezTehUg.new-main`

#### Шаг 4: Настройка параметров деплоя
```
Branch to deploy: main
Build command: npm run netlify
Publish directory: out
```

#### Шаг 5: Настройка домена (опционально)
1. В настройках сайта: "Site settings" → "Domain management"
2. Нажать "Options" → "Edit site name"
3. Ввести желаемое имя: `deztehug` (получится `deztehug.netlify.app`)

#### Шаг 6: Настройка переменных окружения
В настройках сайта: "Environment variables" → добавить:
```
NODE_VERSION=18
NPM_VERSION=9
NEXT_TELEMETRY_DISABLED=1
```

#### Шаг 7: Проверка деплоя
Проверить основные страницы:
- Главная: `https://ваш-сайт.netlify.app/`
- О компании: `https://ваш-сайт.netlify.app/about`
- Калькулятор: `https://ваш-сайт.netlify.app/calculator`
- Услуги: `https://ваш-сайт.netlify.app/services`

---

## 5. 🛑 Временное отключение деплоя на Netlify

### Файл: `NETLIFY_DEPLOYMENT.md` (раздел "Временное отключение деплоя")

#### Метод 1: Полное отключение сайта
1. Войти в панель Netlify
2. Перейти: Site settings → General → Site details
3. Нажать "Stop auto publishing"
4. Подтвердить действие

#### Метод 2: Приостановка автоматических деплоев
1. Перейти: Site settings → Build & deploy → Continuous deployment
2. Нажать "Stop builds"
3. Выбрать "Stop auto publishing"

#### Метод 3: Использование переменной окружения
1. Добавить переменную: Site settings → Environment variables
2. Добавить: `SITE_MAINTENANCE=true`
3. Обновить код приложения (добавить в `app/layout.tsx`)

---

## 6. 🎨 Интеграция анимационных иконок

### Файл: `docs/animated-icons-integration.md`

#### Правила именования файлов:
- Формат: `kebab-case.json`
- Язык: английский
- Расширение: обязательное `.json`
- Примеры: `antibiotic.json`, `insect.json`, `microscope.json`

#### Расположение файлов:
```
public/animations/
├── .gitkeep
├── antibiotic.json
├── insect.json
└── microscope.json
```

#### Использование компонента:
```typescript
import AnimatedIcon from '@/components/AnimatedIcon';

<AnimatedIcon animationName="antibiotic" className="w-16 h-16" />
```

#### Добавление новой анимации:
1. Создать анимацию в Adobe After Effects
2. Экспортировать в формат JSON (Bodymovin или LottieFiles)
3. Оптимизировать размер файла (< 100KB для простых, < 500KB для сложных)
4. Скопировать в `public/animations/`
5. Использовать в компоненте: `<AnimatedIcon animationName="имя-файла" />`

---

## 7. 📐 Структура проекта и иерархия страниц

### Файл: `docs/site-hierarchy.md`

#### Основные страницы:
- **Главная**: `app/page.tsx`
- **О компании**: `app/about/page.tsx`
- **Калькулятор**: `app/calculator/page.tsx`
- **Услуги**: `app/services/page.tsx`

#### Страницы услуг:
- Дезинфекция: `app/services/disinfection/page.tsx`
- Дезинсекция: `app/services/pest-control/page.tsx`
- Дератизация: `app/services/deratization/page.tsx`
- Анализ воды: `app/services/water-analysis/page.tsx`

#### Структура компонентов сервисов:
1. **HeroSection** - Главная секция
2. **ProblemSection** - Секция проблем
3. **SolutionSection** - Секция решений
4. **CTASection** - Призыв к действию
5. **FinalCTASection** - Финальный призыв

---

## 8. 🔍 Отладка и анализ компонентов

### Файл: `DEBUG_FOOTER_REPORT_20250107.md`

#### Критические проблемы производительности:
1. **Inline SVG Pattern** - вынести в отдельный CSS файл
2. **Expensive Blur Effects** - уменьшить радиус blur
3. **Layout Thrashing** - добавить will-change или использовать только transform

#### Проблемы поддерживаемости:
1. **Hardcoded Contact Data** - создать конфигурационный файл
2. **Monolithic Component** - разделить на более мелкие компоненты

#### Проблемы доступности:
1. **Missing Aria Labels** - добавить aria-label для ссылок
2. **Color-Only Indicators** - добавить текстовые или иконочные альтернативы

---

## 9. 💻 Скрипты проекта

### Файл: `package.json`

#### Доступные команды:
```bash
npm run dev      # Запуск dev сервера (localhost:3000)
npm run build    # Сборка проекта для продакшена
npm run netlify  # Сборка для Netlify (аналогично build)
npm run start    # Запуск продакшен сервера
npm run lint     # Проверка кода линтером
```

---

## 10. ⚙️ Конфигурация проекта

### Файл: `next.config.js`

#### Основные настройки:
- `eslint.ignoreDuringBuilds: true` - игнорировать ESLint ошибки при сборке
- `images.unoptimized: true` - не оптимизировать изображения (для статического экспорта)
- `trailingSlash: false` - не добавлять слэш в конце URL

### Файл: `netlify.toml`

#### Настройки деплоя:
- **Build command**: `npm run build`
- **Publish directory**: `out`
- **Node version**: 18
- **NPM version**: 9

#### Настроенные redirects:
- Редиректы для всех страниц услуг
- Обработка 404 ошибок
- Security headers

---

## 11. 📝 Стиль кодирования

### Файл: `.copilot-instructions.md`

#### Правила:
- Использовать функциональные компоненты с TypeScript
- Предпочитать named exports
- Использовать Tailwind CSS для стилизации
- Следовать принципам компонентной архитектуры
- Использовать TypeScript интерфейсы для типизации props

#### Соглашения об именовании:
- **Компоненты**: PascalCase (например, `ServiceIcon`)
- **Файлы компонентов**: PascalCase.tsx
- **Хуки**: camelCase с префиксом `use`
- **Константы**: UPPER_SNAKE_CASE
- **Переменные и функции**: camelCase

#### Предпочтительные паттерны:
- Использовать `interface` вместо `type` для объектов
- Предпочитать композицию наследованию
- Использовать React.FC только когда необходимо
- Деструктуризация props в параметрах функции
- Использовать optional chaining (?.) и nullish coalescing (??)

---

## 12. 🎯 Специфика проекта

### Тема дизайна:
- **Стиль**: Cyberpunk/технологичный дизайн
- **Цветовая схема**: Неоновые цвета (#39FF14, #FF6B35), темный фон
- **Анимации**: Плавные переходы с Framer Motion
- **Иконки**: Кастомные SVG + Lucide React
- **Адаптивность**: Mobile-first подход

### Компоненты для переиспользования:
- `CyberpunkButton` - стилизованные кнопки
- `CyberpunkCard` - карточки в cyberpunk стиле
- `ServiceIcon` - иконки услуг
- `TrustFactorIcon` - иконки факторов доверия
- `WhyChooseUsIcon` - иконки преимуществ

---

## 13. 🔧 Устранение неполадок

### Проблема: Build fails with "Cannot find module"
**Решение:**
```bash
rm -rf node_modules package-lock.json
npm install
npm run build
```

### Проблема: 404 ошибки на страницах услуг
**Решение**: Проверить, что в `netlify.toml` настроены правильные redirects

### Проблема: Медленная загрузка изображений
**Решение**: В `next.config.js` уже настроено `images: { unoptimized: true }`

### Проблема: CSP ошибки в консоли
**Решение**: Настроить Content-Security-Policy в `netlify.toml`

### Проблема: Шрифты не загружаются
**Решение**: Убедиться, что в CSP разрешены `fonts.googleapis.com` и `fonts.gstatic.com`

### Проблема: Copilot не работает
1. Проверить подписку GitHub Copilot в настройках GitHub
2. Убедиться, что расширение включено
3. Перезапустить VS Code
4. Проверить интернет-соединение

---

## 14. 📊 Автоматические деплои

После первоначальной настройки Netlify будет автоматически деплоить сайт при каждом push в ветку `main`:

1. Внести изменения в код
2. Зафиксировать изменения:
```bash
git add .
git commit -m "feat: Описание изменений"
git push origin main
```
3. Netlify автоматически запустит новый деплой

---

## 15. 📈 Мониторинг и оптимизация

### Просмотр логов деплоя:
1. В панели Netlify перейти в "Deploys"
2. Кликнуть на нужный деплой
3. Просмотреть логи в разделе "Deploy log"

### Оптимизация производительности:
- ✅ Статическая генерация всех страниц
- ✅ Оптимизация изображений для статического экспорта
- ✅ Кэширование статических ресурсов
- ✅ Минификация CSS и JavaScript
- ✅ Сжатие шрифтов и ресурсов

### Безопасность:
- ✅ Security headers (CSP, X-Frame-Options, etc.)
- ✅ HTTPS по умолчанию
- ✅ Защита от XSS атак
- ✅ Referrer Policy

---

## 📚 Дополнительные ресурсы

- **Netlify Support**: [support.netlify.com](https://support.netlify.com)
- **Next.js Documentation**: [nextjs.org/docs](https://nextjs.org/docs)
- **Проектная документация**: `docs/`

---

*Последнее обновление: 2025-01-08*  
*Версия документа: 1.0.0*  
*Статус: Активная разработка*

